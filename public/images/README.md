# About photo

Add your portrait here as **`about.jpg`** (recommended: 1200×1500px or similar 4:5 ratio, JPG or WebP).

The site loads it from `src/lib/data.ts` → `site.aboutPhoto` (default: `/images/about.jpg`).

To use a different filename, update `aboutPhoto` in `src/lib/data.ts`.
